# 1. Use map to double each number in [1, 2, 3, 4, 5].
numbers = [1, 2, 3, 4, 5]
doubled_numbers = list(map(lambda x: x * 2, numbers))
print(doubled_numbers)

# 2. Use map to convert ["10", "20", "30"] into integers.
strings = ["10", "20", "30"]
integers = list(map(int, strings))
print(integers)

# 3. Use map with lambda to find the cube of each number in [2, 3, 4].
numbers = [2,3,4]
cube_num=list(map(lambda x:x*x*x ,numbers))
print(cube_num)

# 4. Use map to add 10 to each element in [5, 15, 25].
numbers=[5,15,25]
add_element=list(map(lambda x:x+10,numbers))
print(add_element)

# 5. Use map to convert ["a", "b", "c"] into uppercase letters.
letters = ["a", "b", "c"]
uppercase_letters = list(map(str.upper, letters))
print(uppercase_letters)

# 6. Use map with lambda to find the length of each word in ["data", "science",
# "ai"].
words = ["data", "science", "ai"]
lengths = list(map(len, words))
print(lengths)

# 7. Use map to convert a list of prices [100, 200, 300] into discounted prices
 #by reducing 10%.
li=[100,200,300]
reduce_li=list(map(lambda x:x*0.9,li))
print(reduce_li)

# 8. Use map with lambda to append "!" to each string in ["hi", "hello"].
greetings = ["hi", "hello"]
exclaimed = list(map(lambda s: s + "!", greetings))
print(exclaimed)

# 9. Use map to convert [1, 2, 3, 4] into their string form.
numbers = [1, 2, 3, 4]
string_form = list(map(str, numbers))
print(string_form)

# 10. Use map with lambda to subtract 2 from each number in [10, 20, 30].
nums = [10, 20, 30]
result = list(map(lambda x: x - 2, nums))
print(result)

# 11. Use filter to extract only odd numbers from [1, 2, 3, 4, 5, 6].
def is_odd(n): 
    return n % 2 != 0
result = list(filter(is_odd, [1, 2, 3, 4, 5, 6]))
print(result)

# 12. Use filter with lambda to get numbers greater than 25 from [10, 30,20,40].
result = list(filter(lambda x: x > 25, [10, 30, 20, 40]))
print(result)

# 13. Use filter to keep only positive numbers from [-5, 3, -2, 7, 0].
def is_positive(n): 
    return n > 0
result = list(filter(is_positive, [-5, 3, -2, 7, 0]))
print(result)

# 14. Use filter with lambda to remove all zeros from [0, 1, 0, 2, 3].
result = list(filter(lambda x: x != 0, [0, 1, 0, 2, 3]))
print(result)

# 15. Use filter to select words longer than 3 characters from ["hi","data","ai",
# "python"].
def long(word): 
    return len(word) > 3

result = list(filter(long, ["hi", "data", "ai", "python"]))
print(result)

# 16. Use filter with lambda to keep only numbers divisible by 5 from [10, 12, 15,
# 18, 20].
li = list(filter(lambda x: x % 5 == 0, [10, 12, 15, 18, 20]))
print(li)

# 17. Use filter to select strings that start with 'p' from ["python", "java", "php",
# "c"].
def starts_with_p(s): 
    return s.startswith('p')

result = list(filter(starts_with_p, ["python", "java", "php", "c"]))
print(result)

# 18. Use filter with lambda to extract all even numbers from [11, 22, 33, 44, 55].
result = list(filter(lambda x: x % 2 == 0, [11, 22, 33, 44, 55]))
print(result)

# 19. Use filter to remove empty strings from ["hello", "", "world", ""].
# Using None as the function in filter removes all 'falsy' values (like empty strings)
result = list(filter(None, ["hello", "", "world", ""]))
print(result)

# 20. Use filter with lambda  keep values less than 50 from [10, 60, 30, 80, 40].
result = list(filter(lambda x: x < 50, [10, 60, 30, 80, 40]))
print(result)

# 21. Write a lambda function to check whether a number is positive or negative.
pos_neg = (lambda x: "Positive" if x > 0 else "Negative")
print(pos_neg(5))


# 22. Write a lambda function return "Pass" if marks ≥ 40, else "Fail".
result = lambda marks: "Pass" if marks >= 40 else "Fail"
print(result(67))

# 23. Write a lambda function to find the square of a number.
square = lambda x: x**2
print(square(4))

# 24. Write a lambda function to return "Adult" if age ≥ 18, else "Minor".
check_age = lambda age: "Adult" if age >= 18 else "Minor"
print(check_age(34))

# 25. Write a lambda function to find the maximum of two numbers.
find_max = lambda a, b: a if a > b else b
print(find_max(4,6))

# 26. Write a lambda function to return "Even" or "Odd".
check_even_odd = lambda x: "Even" if x % 2 == 0 else "Odd"
print(check_even_odd(6))

# 27. Use map with a lambda to convert [1, 2, 3, 4] into "Odd" or "Even" labels.
nums = [1, 2, 3, 4]
labels = list(map(lambda x: "Even" if x % 2 == 0 else "Odd", nums))
print(labels)

# 28. Use filter with a lambda to keep only "Even" numbers from [1, 2, 3, 4, 5,
# 6].
nums = [1, 2, 3, 4, 5, 6]
evens = list(filter(lambda x: x % 2 == 0, nums))
print(evens)

# 29. Use map with a lambda to add "Mr. " before each name in ["Ram", "Shyam"].
names = ["Ram", "Shyam"]
prefix = list(map(lambda name: "Mr. " + name, names))
print(prefix)


# 30. Use filter with lambda to select only strings containing the letter 'a' from
# ["cat", "dog", "apple"].
words = ["cat", "dog", "apple"]
has_a = list(filter(lambda s: 'a' in s, words))
print(has_a)

# 31. Use map with a lambda to multiply each number in [2, 4, 6] by 3.
nums = [2, 4, 6]
multiplied = list(map(lambda x: x * 3, nums))
print(multiplied)

# 32. Use filter with a lambda to remove all negative numbers from [-3, -1, 0, 2,
# 5].
nums = [-3, -1, 0, 2, 5]
positives = list(filter(lambda x: x >= 0, nums))
print(positives)

# 33. Use map with a lambda to convert temperatures [0, 20, 30] into Fahrenheit.
celsius = [0, 20, 30]
fahrenheit = list(map(lambda c: (c * 9/5) + 32, celsius))

# 34. Use filter with a lambda to keep only words ending with 'n' from ["python",
# "java", "c", "go"].
words = ["python", "java", "c", "go"]
ends_n = list(filter(lambda s: s.endswith('n'), words))
print(ends_n)

# 35. Use map with lambda to replace each number in [1, 2, 3] with it boolean val
# (True or False).
nums = [1, 2, 3]
bools = list(map(bool, nums)) # Or: list(map(lambda x: bool(x), nums))
print(bools)